{
  "name": "ellis-travel-agent",
  "private": true,
  "workspaces": ["src/client", "src/server"],
  "scripts": {
    "dev": "concurrently \"bun run dev --filter=client\" \"bun run dev --filter=server\"",
    "build": "bun run build --filter=client && bun run build --filter=server",
    "start": "bun run start --filter=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
